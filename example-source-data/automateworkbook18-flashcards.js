const TOPIC = "Automate the Boring Stuff Workbook, Chapter 18 - CSV, JSON, and XML Files";
let JUSTIFICATION = "left"; // "left", "right", or "center"
let FLASHCARDS = [
  ["Of the CSV, JSON, and XML formats, which most closely resembles a spreadsheet?", "The CSV format most closely resembles a spreadsheet."],
  ["What does CSV stand for?", "CSV stands for comma-separated values. It's called that because the values in the file are separated by commas."],
  ["Are CSV files plaintext files? Can you view them in a text editor such as Notepad or TextEdit?", "Yes. CSV files are plaintext files, and you can view them in a text editor such as Notepad or TextEdit."],
  ["What data types do CSV files support?", "Text strings are the only data type CSV files support."],
  ["CSV files don't have as many features as Excel spreadsheet files. What is their main advantage?", "The main advantage of CSV files compared to Excel spreadsheets is their simplicity."],
  ["True or false: The data in a CSV file cannot include commas.", "False. CSV files can include commas in their data, so long as they're escaped with slashes. The csv module handles this escaping for you automatically."],
  ["Passing the filename of a CSV file to <code>csv.reader()</code> and <code>csv.writer()</code> doesn't seem to work. Why?", "You must pass a File object (like the one returned by <code>open()</code>) to <code>csv.reader()</code> and <code>csv.writer()</code>, not a filename string."],
  ["The <code>csv.reader()</code> function returns a reader object. How can you get a list value containing all the rows of data from this object?", "Pass the reader object to the <code>list()</code> function, as in <code>list(reader_obj)</code>."],
  ["The <code>csv.reader()</code> function returns a reader object. How can you loop over each row of data from this object?", "Use a for loop to iterate over the reader object, as in <code>for row in reader_obj:</code>."],
  ["Assume the reader object's data has been converted into a list value stored in a variable named <code>example_data</code>.<br>Which row and column does <code>example_data[6][1]</code> access?", "<code>example_data[6][1]</code> accesses the data in the seventh row and second column."],
  ["What data type can you pass to the <code>writerow()</code> method of writer objects?", "The <code>writerow()</code> method accepts a list of strings."],
  ["A reader object represents each row as a list of strings. How does a DictReader object represent each row?", "A DictReader object represents each row as a dictionary."],
  ["When you read a CSV file with a DictReader object, where does the object get its keys?", "The keys in the dictionaries of a DictReader object come from the first row of the CSV file."],
  ["What data type can you pass to the <code>writerow()</code> method of DictWriter objects?", "The <code>writerow()</code> method of DictWriter objects accepts a dictionary."],
  ["What is wrong with the following code?<br><pre>file_obj = open('example.csv')\nwriter = csv.writer(file_obj)</pre>", "The File object passed to <code>csv.writer()</code> was opened in read mode instead of write mode."],
  ["What does TSV stand for?", "TSV stands for tab-separated values."],
  ["How can you make the csv module read a TSV file?", "Pass the <code>delimiter='\\t'</code> keyword argument to <code>csv.reader()</code> or <code>csv.writer()</code>."],
  ["What is one term for dictionary-like data structures used in other programming languages?", "In other programming languages, dictionary-like data structures are called mappings, hash maps, hash tables, or associative arrays."],
  ["What is a term for list-like data structures used in other programming languages?", "In other programming languages, list-like data structures are called arrays."],
  ["Plaintext formats like JSON and XML aren't disk-space efficient. What advantage do they have?", "Plaintext formats like JSON and XML have the benefit of being easy to read by humans using text editors."],
  ["What does JSON stand for?", "JSON stands for JavaScript Object Notation."],
  ["What does XML stand for?", "XML stands for eXtensible Markup Language."],
  ["Which format resembles Python syntax more closely, JSON or XML?", "JSON resembles Python syntax more closely than XML."],
  ["Of <code>['cat', 'dog',]</code> and <code>[\"cat\", \"dog\"]</code>, which is Python syntax and which is JSON syntax?", "<code>['cat', 'dog',]</code> is Python syntax and <code>[\"cat\", \"dog\"]</code> is JSON syntax, because trailing commas aren't allowed in JSON, and JSON requires double quotes for strings."],
  ["Of <code>[True, False]</code> and <code>[true, false]</code>, which is Python syntax and which is JSON syntax?", "<code>[True, False]</code> is Python syntax because Python capitalizes Boolean values, while <code>[true, false]</code> is JSON syntax because JavaScript uses lowercase Boolean values."],
  ["Which format do APIs usually use to deliver their responses, JSON or XML?", "APIs usually deliver JSON data instead of XML data."],
  ["In the JSON module, what does the 's' in <code>loads()</code> and <code>dumps()</code> stand for?", "The 's' stands for string. The <code>json.loads()</code> function loads a string of JSON-formatted text, and <code>json.dumps()</code> returns a string of JSON-formatted text."],
  ["What code takes the Python dictionary <code>{'temperature': 72}</code> and returns the Python string <code>'{\"temperature\": 72}'</code>?", "<code>json.dumps({'temperature': 72})</code>"],
  ["What other markup language does XML syntax resemble?", "XML syntax resembles HTML syntax."],
  ["What is the closing XML tag for <code>&lt;spam&gt;</code>?", "<code>&lt;/spam&gt;</code> is the closing XML tag for <code>&lt;spam&gt;</code>."],
  ["Why is <code>&lt;person&gt;&lt;name&gt;Alice Doe&lt;/person&gt;&lt;/name&gt;</code> invalid XML syntax?", "The XML syntax is invalid because the <code>&lt;/person&gt;</code> and <code>&lt;/name&gt;</code> closing tags are in the wrong order."],
  ["Write the following Python data as JSON: <code>{\"address\": {\"street\": \"100 Larkin St.\", \"city\": \"San Francisco\", \"zip\": \"94102\"}}</code>.", "The JSON syntax is the same as the Python syntax in this case: <code>{\"address\": {\"street\": \"100 Larkin St.\", \"city\": \"San Francisco\", \"zip\": \"94102\"}}</code>."],
  ["Write the following Python data as XML: <code>{\"address\": {\"street\": \"100 Larkin St.\", \"city\": \"San Francisco\", \"zip\": \"94102\"}}</code>.", "<code>&lt;address&gt;&lt;street&gt;100 Larkin St.&lt;/street&gt;&lt;city&gt;San Francisco&lt;/city&gt;&lt;zip&gt;94102&lt;/zip&gt;&lt;/address&gt;</code>"],
  ["What are the names of the XML attributes in the following XML: <code>&lt;address street=\"100 Larkin St.\" city=\"San Francisco\" zip=\"94102\" /&gt;</code>?", "The XML attribute names are street, city, and zip."],
  ["What is the name of the first element in an XML document that contains all other elements?", "The root element is the first element in an XML document that contains all other elements."],
  ["What does DOM stand for?", "DOM stands for Document Object Model."],
  ["What does SAX stand for?", "SAX stands for Simple API for XML."],
  ["What is the name of the approach to reading XML documents that reads XML elements one at a time, instead of loading the entire XML document into memory?", "SAX is the approach of reading XML documents one element at a time."],
  ["What is the benefit of the DOM approach of reading XML documents?", "The benefit of the DOM approach of reading XML documents entirely into memory is convenience; you can access any XML element at any time."],
  ["After running <code>import xml.etree.ElementTree as ET</code>, what function can you call to obtain an Element object of the root element in a string of XML?", "The <code>ET.fromstring()</code> function returns the Element object for the root element in the XML string passed to it."],
  ["What is wrong with this code: <code>ET.tostring('&lt;person&gt;Albert&lt;/person&gt;', encoding='utf-8')</code>?", "The <code>tostring()</code> function doesn't take string arguments. Instead, you must pass it an <code>ET.Element</code> object: <code>ET.tostring(ET.Element('person'), encoding='utf-8')</code>."],
  ["The <code>tostring()</code> function returns a bytes object instead of a string. If the <code>person</code> variable contains an Element object, what code returns the XML string of this element?", "<code>ET.tostring(person, encoding='utf-8').decode('utf-8')</code> decodes the bytes object and returns a string."],
  ["Assume <code>root</code> stores an Element object. What does the following code do?<br><pre>for elem in root.iter():\n    print(elem.tag, elem.text)</pre>", "This code iterates over every element in the XML, printing the element name and the text in between the element's opening and closing tags."],
  ["Assume <code>root</code> stores an Element object. What does the following code do?<br><pre>for elem in root.iter('number'):\n    print(elem.tag, elem.text)</pre>", "This code iterates over every <code>&lt;number&gt;</code> element in the XML, printing the element name ('number') and the text in between the <code>&lt;number&gt;</code> and <code>&lt;/number&gt;</code> tags."],
  ["Assume you have an Element object for the XML <code>&lt;person&gt;&lt;/person&gt;</code>. What is the value of its <code>text</code> attribute, given the element has no text?", "The <code>text</code> attribute is set to <code>None</code>."]
];
