const TOPIC = "Automate the Boring Stuff Workbook, Chapter 17 - PDF and Word Documents";
let JUSTIFICATION = "left"; // "left", "right", or "center"
let FLASHCARDS = [
  ["What do you pass to the <code>pypdf.PdfReader()</code> function to open a PDF file?", "Pass a string filename to the <code>pypdf.PdfReader()</code> function to open a PDF file."],
  ["Where can you find the individual Page objects of a PdfReader object?", "The <code>pages</code> attribute of PdfReader objects stores the individual Page objects."],
  ["Write the code for a function named <code>get_num_pages()</code> that accepts a PDF's filename as a string and returns the number of pages it has.", "<pre>def get_num_pages(filename):\n    reader = pypdf.PdfReader(filename)\n    return len(reader.pages)</pre>"],
  ["Which method of Page objects extracts text from a PDF?", "The <code>extract_text()</code> method of Page objects extracts text from a PDF."],
  ["Assume a variable named <code>reader</code> contains the PdfReader object.<br>Write code that extracts the text of page 2 of a PDF file.", "<code>reader.pages[1].extract_text()</code>"],
  ["Which pdfminer function extracts text from a PDF file, and which argument must you pass to this function?", "The <code>pdfminer.high_level.extract_text()</code> function extracts text from a PDF when you pass it the PDF's filename as a string."],
  ["How can you automatically clean up the extracted text strings from a PDF in a way that respects the context of the text?", "LLMs such as ChatGPT can automatically clean up the extracted text strings from a PDF."],
  ["Which pypdf function lets you create new PDF files?", "The <code>pypdf.PdfWriter()</code> function lets you create new PDF files."],
  ["Can the PyPDF package write arbitrary text to a PDF file in the same way that Python can write arbitrary text to a .txt file?", "No. The PyPDF package cannot write arbitrary text to a PDF file the way Python can write arbitrary text to a .txt file."],
  ["Can you rotate a page in a PDF by 45 degrees using pypdf or pdfminer?", "No. You cannot rotate a page by 45 degrees using pypdf or pdfminer. These modules can rotate pages in 90-degree increments only."],
  ["Write code that creates a new file named rotated.pdf that has the contents of example.pdf, but rotated clockwise by 90 degrees.", "<pre>import pypdf\nwriter = pypdf.PdfWriter()\nwriter.append('example.pdf')\nfor i in range(len(writer.pages)):\n    writer.pages[i].rotate(90)\nwith open('rotated.pdf', 'wb') as file:\n    writer.write(file)</pre>"],
  ["What Page method allows you to add a watermark to a page?", "The <code>merge_page()</code> method of Page objects allows you to add a watermark to a page."],
  ["What PdfWriter method adds a blank page to the end of a PDF document?", "The <code>add_blank_page()</code> method adds a blank page to the end of a PDF."],
  ["Assume a variable named <code>writer</code> contains the PdfWriter object.<br>Write the code to insert a blank page as the new page 3 in a PDF document.", "<code>writer.insert_blank_page(index=2)</code>"],
  ["Which modern encryption algorithm do experts recommend you use to encrypt your PDF files?", "Use the 'AES-256' encryption algorithm to encrypt your PDF files."],
  ["Why would 'elephant' be a poor password to use to encrypt your PDF files?", "The password 'elephant' is a poor choice because it is an English word and can easily be brute-forced."],
  ["What two types of passwords do PDF files support for encryption?", "PDF files support a user password for viewing the PDF and an owner password for printing, commenting, extracting text, and taking other actions."],
  ["Write code that opens a file named demo.docx and stores the Document object in a variable named <code>doc</code>.", "<code>doc = docx.Document('demo.docx')</code>"],
  ["What code would get a string value of the text in the second paragraph of a Document object?", "<code>doc.paragraphs[1].text</code>"],
  ["What code would retrieve the number of paragraphs in a Document object?", "<code>len(doc.paragraphs)</code>"],
  ["True or false: Document objects contain Paragraph objects, which in turn contain Run objects.", "True. Document objects contain Paragraph objects, which in turn contain Run objects."],
  ["True or false: To italicize some text in a paragraph and bold some other text in that same paragraph, you must set the bold and italic attributes of the Paragraph object to True.", "False. Setting the Paragraph object's bold and italic attributes to True will set the entire text of the paragraph to bold and italic. To italicize some text in a paragraph and bold some other text, you must set the bold and italic attributes of the Run objects in the Paragraph object to True."],
  ["Which of the following have a text attribute: Document objects, Paragraph objects, or Run objects?", "Paragraph objects and Run objects have text attributes, while Document objects do not."],
  ["To what three values can you set attributes of Run objects such as bold, italic, and strike, and what do these values mean?", "You can set bold, italic, strike, and other attributes to <code>True</code> (meaning always enabled), <code>False</code> (meaning always disabled), or <code>None</code> (meaning enabled or disabled based on the Run object's existing style)."],
  ["What code adds a paragraph to a document with the text \"Hello, world!\" in the built-in Title style?", "<code>doc.add_paragraph('Hello, world!', 'Title')</code>"],
  ["What kind of objects have the <code>add_paragraph()</code> method?", "Document objects have the <code>add_paragraph()</code> method."],
  ["What kind of objects have the <code>add_run()</code> method?", "Paragraph objects have the <code>add_run()</code> method."],
  ["Create a blank .docx document in either Microsoft 365 or another application. Then, open it with Python-Docx. How many Paragraph objects does this empty document contain? How many Run objects does this empty document contain?", "A blank .docx document has one Paragraph object that contains zero Run objects."],
  ["Write a program that creates a Word document named millionstars.docx that has exactly one million asterisks; no more, no less.", "<pre>import docx\ndoc = docx.Document()\ndoc.add_paragraph('*' * 1000000)\ndoc.save('millionstars.docx')</pre>"],
  ["Write a program that creates a Word document named countdown.docx that counts down from 1,000 to 0, with one number per paragraph.", "<pre>import docx\ndoc = docx.Document()\nfor number in range(1000, -1, -1):\n    doc.add_paragraph(str(number))\ndoc.save('countdown.docx')</pre>"]
];
