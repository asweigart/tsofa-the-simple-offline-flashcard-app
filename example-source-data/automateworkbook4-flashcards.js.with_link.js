const TOPIC = "Automate the Boring Stuff Workbook, Chapter 4 - Functions";
let JUSTIFICATION = "left"; // "left", "right", or "center"
const TOPIC = "Automate Workbook";
let FLASHCARDS = [
  ["Answer \"yes\" if it is a valid Python def statement; answer \"no\" if it is invalid.<br><code>def hello:</code>", "No. The parentheses after hello are missing."],
  ["Answer \"yes\" if it is a valid Python def statement; answer \"no\" if it is invalid.<br><code>define hello(name):</code>", "No. The keyword is def and not define."],
  ["Answer \"yes\" if it is a valid Python def statement; answer \"no\" if it is invalid.<br><code>def h(name):</code>", "Yes."],
  ["Answer \"yes\" if it is a valid Python def statement; answer \"no\" if it is invalid.<br><code>hello(name):</code>", "No. The def keyword is missing."],
  ["Answer \"yes\" if it is a valid Python def statement; answer \"no\" if it is invalid.<br><code>def:</code>", "No. The function name and parentheses are missing."],
  ["Answer \"yes\" if it is a valid Python def statement; answer \"no\" if it is invalid.<br><code>def hello():</code>", "Yes."],
  ["Answer \"yes\" if it is a valid Python def statement; answer \"no\" if it is invalid.<br><code>def hello(name):</code>", "Yes."],
  ["How can you tell that the following code defines a function rather than calls a function?<br><code>def say_hello():</code>", "This function definition has the def keyword and a colon, which a function call does not have."],
  ["What are the parameters in the following def statement?<br><code>def add_club_member(first_name, last_name):</code>", "The two parameters are first_name and last_name."],
  ["In the following code, is 'Albert' a parameter or an argument?<br><code>say_hello('Albert')</code>", "The 'Albert' value is an argument. Parameters are variables, not values."],
  ["Describe what this complete program prints.<br><pre>def say_hello():\n    print('Hello')</pre>", "This program prints nothing, because the say_hello() function is defined but never called."],
  ["Describe what this complete program prints.<br><pre>def say_hello():\n    print('Hello')\nfor i in range(3):\n    say_hello()</pre>", "This program prints the string Hello three times."],
  ["Describe what this complete program prints.<br><pre>def say_hello():\n    for i in range(3):\n        print('Hello')\nsay_hello()\nsay_hello()</pre>", "This program prints the string Hello six times (three times each for the two function calls)."],
  ["What is the data type of the return value in the following function?<br><pre>def enter_password(password):\n    if password == 'swordfish':\n        return True\n    else:\n        return False</pre>", "The data type of the return value is a Boolean or bool value."],
  ["In the enter_password() function, what can the data type of the password parameter be?<br><pre>def enter_password(password):\n    if password == 'swordfish':\n        return True\n    else:\n        return False</pre>", "Technically, the password parameter can have a value of any data type, though the code suggests that it should be a string."],
  ["What is the data type of the return value in the following function?<br><pre>def get_greeting():\n    print('What is your name?')\n    name = input()\n    return 'Hello, ' + name</pre>", "The data type of the return value is a string, because 'Hello, ' is a string and name is also a string (since the return values of input() are always strings), and concatenating two strings with the + operator always evaluates to a string."],
  ["Does the following expression evaluate to True or False?<br><code>None == True</code>", "False"],
  ["Does the following expression evaluate to True or False?<br><code>None == False</code>", "False"],
  ["Does the following expression evaluate to True or False?<br><code>None == 'None'</code>", "False"],
  ["Does the following expression evaluate to True or False?<br><code>None == None</code>", "True"],
  ["Does the following expression evaluate to True or False?<br><code>None == 'hello'</code>", "False"],
  ["Does the following expression evaluate to True or False?<br><code>None == 0</code>", "False"],
  ["Does the following expression evaluate to True or False?<br><code>None == -1.5</code>", "False"],
  ["What does a stack frame object represent?", "A stack frame object represents a function call."],
  ["When is a stack frame object pushed to the top of the call stack?", "A stack frame object is pushed to the top of the call stack when a function is called."],
  ["When is a stack frame object popped off the top of the call stack?", "A stack frame object is popped off the top of the call stack when the function call returns."],
  ["What does the stack frame object at the top of the call stack represent?", "The stack frame object at the top of the call stack represents the function call the execution is currently in."],
  ["A call to a function named spam() is made. Then, a call to an eggs() function is made. Next, eggs() returns. After that, a call to a bacon() function is made. What does the call stack look like at this point?", "The call stack has a stack frame object for the spam() function call on the bottom, with a stack frame object for the bacon() function call on top of it."],
  ["A program has absolutely no function calls in it. What does the call stack look like while the program runs?", "A program that has absolutely no function calls in it always has an empty call stack with no stack frame objects for local variables."],
  ["Are function parameters global variables or local variables?", "Local. Function parameters are always local variables."],
  ["A variable in a function is marked with the global statement. Is it a global or local variable?", "Global. Variables marked with the global statement are always global."],
  ["Can a variable be both global and local?", "No. A variable must be global or local and can't be both."],
  ["If a global spam variable exists, and a function has a <code>spam = 42</code> assignment statement and no <code>global spam</code> statement, is the spam variable in the function local or global?", "Local. If a variable in a function is used in an assignment statement in that function and there is no global statement for it, it is a local variable."],
  ["If a global spam variable exists, and a function has a <code>spam = 42</code> assignment statement as well as a <code>global spam</code> statement, is the spam variable in the function local or global?", "Global. Variables marked with the global statement are always global."],
  ["If a global spam variable exists, and a function never assigns spam a value and has no <code>global spam</code> statement, the function uses the spam variable (such as in <code>print(spam)</code>). Is the spam variable in the function local or global?", "Global. If a variable in a function is used in a function but never in an assignment statement, it is a global variable. In this case, it is global even if there is no global statement for it."],
  ["Determine what this program outputs.<br><pre>def func(spam):\n    print(spam)\nspam = 'dog'\nfunc('cat')</pre>", "The code prints the string 'cat' because spam in the func() function is a parameter and parameters are always local."],
  ["Determine what this program outputs.<br><pre>def func(eggs):\n    print(spam)\nspam = 'dog'\nfunc('cat')</pre>", "The code prints the string 'dog' because spam in the func() function is not used in an assignment statement. Note that the parameter is eggs, but spam is the variable passed to print(), so the value in eggs doesn't matter."],
  ["Determine what this program outputs.<br><pre>def func():\n    spam = 'cat'\nspam = 'dog'\nfunc()\nprint(spam)</pre>", "The code prints the string 'dog' because spam in the func() function is used in an assignment statement, making it a local variable. The spam = 'cat' statement applies to a local spam variable and not to the global spam variable."],
  ["Determine what this program outputs.<br><pre>def func():\n    global spam\n    spam = 'cat'\nspam = 'dog'\nfunc()\nprint(spam)</pre>", "The code prints the string 'cat' because spam in the func() function is used in a global statement, making it a global variable. The spam = 'cat' statement applies to the global spam variable."],
  ["Determine what this program outputs.<br><pre>def func():\n    global spam\n    print(spam)\n    spam = 'cat'\nspam = 'dog'\nfunc()</pre>", "The code prints the string 'dog' because spam in the func() function is used in a global statement, making it a global variable. The print() function prints spam before it was changed to 'cat'."],
  ["Determine what this program outputs.<br><pre>def func():\n    print(spam)\n    spam = 'cat'\nspam = 'dog'\nfunc()</pre>", "The code prints nothing, because it crashes with an error. If a function contains an assignment statement for a variable that is not used in a global statement, the variable is local. However, the print(spam) line tries to print this local variable before it has been assigned a value. This causes an UnboundLocalError error."],
  ["Would this program crash if the user entered a non-number?<br><pre>print('Enter a number:')\nnumber = int(input())\ntry:\n    print('You entered a number.')\nexcept:\n    print('You did not enter a number.')</pre>", "Yes. The program would crash because the int(input()) call is outside the try block."],
  ["Would this program crash if the user entered a non-number?<br><pre>print('Enter a number:')\ntry:\n    number = int(input())\n    print('You entered a number.')\nexcept ValueError:\n    print('You did not enter a number.')</pre>", "No. The program would not crash, because the int(input()) call is inside the try block, and the except block catches the ValueError exceptions that int() would raise."],
  ["Would this program crash if the user entered a non-number?<br><pre>print('Enter a number:')\ntry:\n    number = int(input())\n    print('You entered a number.')\nexcept ZeroDivisionError:\n    print('You did not enter a number.')</pre>", "Yes. The program would crash, because even though the int(input()) calls are inside the try block, the except block catches only ZeroDivisionError exceptions, and the int() function raises ValueError exceptions."]
];