const TOPIC = "Automate the Boring Stuff Workbook, Chapter 6 - Lists";
let JUSTIFICATION = "left"; // "left", "right", or "center"
let FLASHCARDS = [
  ["What is the first index of any list?", "0. In Python and most programming languages, 0, not 1, is the first index."],
  ["If a variable named spam contains ['cat', 'bat', 'rat', 'hat'], what does <code>spam[3]</code> evaluate to?", "'hat'. If you thought the answer was 'rat', you forgot that the first index is 0, not 1."],
  ["If a variable named spam contains ['cat', 'bat', 'rat', 'hat'], what does <code>spam[4]</code> evaluate to?", "This is a trick question! spam[4] would raise a \"list index out of range\" error because 3 is the last index in spam."],
  ["Do all the values in a Python list need to be of the same data type?", "No. Python lists do not need to contain only values of one data type. This is a restriction in other programming languages, but not in Python."],
  ["If a variable named spam contains an empty list, what happens when <code>spam[0]</code> is evaluated?", "Python raises a \"list index out of range\" error when evaluating spam[0] and spam is an empty list."],
  ["In the expression <code>spam[3]</code>, is the [3] also a list?", "No. For spam[3], the [3] is meant to be the index 3 for the list in spam. Even though it uses square brackets, it is not itself a list."],
  ["What negative index is equivalent to the index in <code>spam[len(spam) - 1]</code>?", "The index -1."],
  ["What negative index is equivalent to the index in <code>spam[len(spam) - 3]</code>?", "The index -3."],
  ["If a variable named spam contains a list, what is the difference between the statement <code>del spam[0]</code> and the statement <code>del spam</code>?", "The statement del spam[0] removes the first value in the spam list while del spam deletes the entire list."],
  ["Determine what this program prints.<br><pre>spam = ['cat', 'dog', 'moose']\nfor i in spam:\n    print(i)</pre>", "The program prints the values in the spam list:<br>cat<br>dog<br>moose"],
  ["Determine what this program prints.<br><pre>spam = ['cat', 'dog', 'moose']\nfor i in range(len(spam)):\n    print(i)</pre>", "The program prints the indexes of the spam list:<br>0<br>1<br>2"],
  ["Determine what this program prints.<br><pre>spam = ['cat', 'dog', 'moose']\nfor i in range(len(spam)):\n    print(spam[i])</pre>", "Even though the program loops over the indexes of the spam list, the actual output comprises the values in the spam list:<br>cat<br>dog<br>moose"],
  ["If an expression is using the in and not in operators, what data type does it evaluate to?", "A Boolean True or False value. For example, 'fish' in ['cat', 'dog', 'moose'] evaluates to False."],
  ["If spam contains the list ['cat', 'dog', 'moose'] and Python runs the statement <code>a, b, c = spam</code>, what does the b variable contain?", "The variable b contains the string 'dog'. This kind of assignment is called unpacking, where the three values in ['cat', 'dog', 'moose'] are assigned to the variables a, b, and c, respectively."],
  ["If Python runs the statement <code>a, b, c = 'cat'</code>, what does the b variable contain?", "The variable b contains the string 'a'. The string 'cat' is also a sequence, so it can be unpacked just like a list."],
  ["Say spam contains a list value and Python runs the statement <code>for a, b in enumerate(spam):</code>. Describe the data that the a and b variables contain.", "The a variable is assigned each index of the spam list, starting at 0. The b variable is assigned that index's value."],
  ["What does the random.choice() function return?", "The random.choice() function returns a randomly selected value from the list passed to it. For example, random.choice(['cat', 'dog']) could return either 'cat' or 'dog'."],
  ["What does the random.shuffle() function do?", "The random.shuffle() function randomizes the order of the values in the list passed to it. For example, if spam = ['cat', 'dog', 'moose'], then random.shuffle(spam) could set spam with the value ['moose', 'cat', 'dog']. The list is modified by the function in place and the function doesn't return any values."],
  ["If spam contains the list ['cat', 'dog', 'moose'] and Python runs <code>import random</code> and <code>random.shuffle(spam)</code>, what does the expression <code>len(spam)</code> evaluate to?", "The expression len(spam) evaluates to 3 because only the order of the values in the list has changed, not the number of values."],
  ["What does this program print?<br><pre>spam = 100\nfor i in range(5):\n    spam += 1\nprint(spam)</pre>", "The program prints 105."],
  ["Rewrite the following assignment statement using the equivalent augmented assignment operator.<br><code>spam = spam * 2</code>", "<code>spam *= 2</code>"],
  ["Rewrite the following assignment statement using the equivalent augmented assignment operator.<br><code>bacon = bacon - 3</code>", "<code>bacon -= 3</code>"],
  ["Rewrite the following assignment statement using the equivalent augmented assignment operator.<br><code>eggs = eggs + bacon * 5</code>", "<code>eggs += bacon * 5</code>"],
  ["Rewrite the following assignment statement using the equivalent augmented assignment operator.<br><code>eggs = eggs * bacon + 5</code>", "<code>eggs *= bacon + 5</code>"],
  ["Rewrite the following assignment statement using the equivalent augmented assignment operator.<br><code>spam = spam + 'LastName'</code>", "<code>spam += 'LastName'</code>"],
  ["Identify the following as either a function or a method.<br><code>sort()</code>", "sort() is a list method."],
  ["Identify the following as either a function or a method.<br><code>len()</code>", "len() is a function."],
  ["Identify the following as either a function or a method.<br><code>append()</code>", "append() is a list method."],
  ["Identify the following as either a function or a method.<br><code>index()</code>", "index() is a list and string method."],
  ["Identify the following as either a function or a method.<br><code>print()</code>", "print() is a function."],
  ["Identify the following as either a function or a method.<br><code>input()</code>", "input() is a function."],
  ["Identify the following as either a function or a method.<br><code>reverse()</code>", "reverse() is a list method."],
  ["Both the remove() list method and the del operator can remove items from a list value. How do they work differently?", "The remove() method removes a value by the value (for example, spam.remove('cat')) while the del operator removes a value by the index (for example, del spam[3])."],
  ["If the spam variable contains a list, running <code>sort(spam)</code> causes an error message. Why?", "The code sort(spam) causes an error because sort() is a list method and not a function. The correct way to call it is spam.sort()."],
  ["If the spam variable contains a list, what code would rearrange the items in spam in \"ASCIIbetical\" order?", "The code <code>spam.sort()</code> arranges the values in spam in \"ASCIIbetical\" order."],
  ["What code could we run so that spam's contents are sorted in alphabetical order?", "The code <code>spam.sort(key=str.lower)</code> sorts a list in alphabetical order."],
  ["Determine what gets printed.<br><pre>>>> spam = ['cat', 'dog', 'moose']\n>>> spam.sort()\n>>> print(spam)</pre>", "['cat', 'dog', 'moose'] because the list was already in \"ASCIIbetical\" order."],
  ["Determine what gets printed.<br><pre>>>> spam = ['cat', 'dog', 'moose']\n>>> spam.sort(reverse=True)\n>>> print(spam)</pre>", "['moose', 'dog', 'cat'] because the reverse=True keyword argument makes the sort() method sort in reverse order."],
  ["Determine what gets printed.<br><pre>>>> spam = [3, 99, 86, 42]\n>>> spam.reverse()\n>>> print(spam)</pre>", "[42, 86, 99, 3] because the reverse() list method reverses the order of values in a list."],
  ["Answer \"Hello\" if the expression prints \"Hello\"; answer \"Nothing\" if it prints nothing.<br><code>True and print('Hello')</code>", "Hello."],
  ["Answer \"Hello\" if the expression prints \"Hello\"; answer \"Nothing\" if it prints nothing.<br><code>False and print('Hello')</code>", "Nothing."],
  ["Answer \"Hello\" if the expression prints \"Hello\"; answer \"Nothing\" if it prints nothing.<br><code>True or print('Hello')</code>", "Nothing."],
  ["Answer \"Hello\" if the expression prints \"Hello\"; answer \"Nothing\" if it prints nothing.<br><code>False or print('Hello')</code>", "Hello."],
  ["Answer \"Hello\" if the expression prints \"Hello\"; answer \"Nothing\" if it prints nothing.<br><code>print('Hello') and True</code>", "Hello."],
  ["Answer \"Hello\" if the expression prints \"Hello\"; answer \"Nothing\" if it prints nothing.<br><code>print('Hello') and False</code>", "Hello."],
  ["Answer \"Hello\" if the expression prints \"Hello\"; answer \"Nothing\" if it prints nothing.<br><code>print('Hello') or True</code>", "Hello."],
  ["Answer \"Hello\" if the expression prints \"Hello\"; answer \"Nothing\" if it prints nothing.<br><code>print('Hello') or False</code>", "Hello."],
  ["List at least two different sequence data types in Python.", "Sequence data types include lists, strings, and tuples."],
  ["Why doesn't the expression <code>'Zophie'[1]</code> evaluate to Z if Z is the first character in the string 'Zophie'?", "Because the first index is 0, not 1. The index 1 refers to the second character in the 'Zophie' string, o."],
  ["What does the expression <code>'Zophie'[-1]</code> evaluate to?", "The expression 'Zophie'[-1] evaluates to the last index in the string, e."],
  ["What does the expression <code>'Zophie'[9999]</code> evaluate to?", "This is a trick question! 'Zophie'[9999] results in an IndexError: string index out of range because there is no index 9999 in a string of only six characters."],
  ["Determine what gets printed.<br><pre>>>> for i in 'cat':\n...     print(i)\n...</pre>", "The code prints each character of the string separately:<br>c<br>a<br>t"],
  ["Determine what gets printed.<br><pre>>>> for i in [['cat', 'dog'], 'moose']:\n...     print(i)\n...</pre>", "The code prints the first value, the list ['cat', 'dog'], and then the second value, the string 'moose':<br>['cat', 'dog']<br>moose"],
  ["Determine what gets printed.<br><pre>>>> for i in 'moose'[0:3]:\n...     print(i)\n...</pre>", "Because 'moose'[0:3] evaluates to the string 'moo', the code prints:<br>m<br>o<br>o"],
  ["Aside from the square brackets and parentheses, what is the main difference between lists and tuples?", "The main difference between lists and tuples is that the contents of a tuple cannot be modified but the contents of a list can be."],
  ["Write the code that obtains a list value from the tuple ('cat', 'dog').", "<code>list(('cat', 'dog'))</code> returns a list form of the tuple."],
  ["Write the code that obtains a tuple value from the list ['cat', 'dog'].", "<code>tuple(['cat', 'dog'])</code> returns a tuple form of the list."],
  ["What happens if you run this code?<br><pre>spam = ('cat', 'dog', 'moose')\nspam[2] = 'cow'</pre>", "You get the error message TypeError: 'tuple' object does not support item assignment because the contents of tuples cannot be modified."],
  ["In Python, variables never contain values. What do they contain?", "Technically, Python variables always contain references to values rather than the values themselves."],
  ["In Python, the = assignment operator never copies values. What does it copy?", "In Python, the = assignment operator always copies references to values rather than the values themselves."],
  ["How many copies of the list value exist in the computer's memory when you run the following code?<br><pre>a = ['cat', 'dog', 'moose']\nb = a\nc = a</pre>", "There is only one list value in the computer's memory; the a, b, and c variables all contain copies of the same reference to this one list value."],
  ["How many copies of the list value exist in the computer's memory when you run the following code?<br><pre>import copy\na = ['cat', 'dog', 'moose']\nb = copy.copy(a)\nc = copy.copy(a)</pre>", "There are three separate list values in the computer's memory, as the copy() method creates a copy of the list instead of just copying a reference to the list."],
  ["Which method would you call to copy the value [['cat', 'dog'], 'moose']: the copy.copy() function or the copy.deepcopy() function?", "Use deepcopy() to make a copy of [['cat', 'dog'], 'moose'], as it is a list that contains other list values."]
];
