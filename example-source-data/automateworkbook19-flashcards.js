const TOPIC = "Automate the Boring Stuff Workbook, Chapter 19 - Keeping Time, Scheduling Tasks, and Launching Programs";
let JUSTIFICATION = "left"; // "left", "right", or "center"
let FLASHCARDS = [
  ["What time zone is the Unix epoch timestamp in?", "The Unix epoch timestamp is in the UTC time zone."],
  ["What function returns a string of the current time, like 'Tue Mar 17 11:05:45 2026'?", "The <code>time.ctime()</code> function returns a string of the current time."],
  ["What function returns a float of the current time, like 1773813875.3518236?", "The <code>time.time()</code> function returns a float of the current time."],
  ["Say you've run <code>import time</code>. What is <code>time</code>? What is <code>time.time()</code>?", "<code>time</code> is a module, and <code>time.time()</code> is a function call to the <code>time()</code> function inside the time module."],
  ["What does the expression <code>time.time() + 10</code> evaluate to?", "The expression <code>time.time() + 10</code> evaluates to a timestamp 10 seconds after the current time."],
  ["What does the expression <code>time.ctime(time.time() - 10)</code> evaluate to?", "The expression <code>time.ctime(time.time() - 10)</code> evaluates to a timestamp 10 seconds ago."],
  ["What is profiling? What does it mean to profile code?", "Profiling code means measuring how long it takes to run. This can be helpful for finding performance bottlenecks or estimating how long processing a larger amount of data might take."],
  ["How do you make your program pause its execution for one-half of a second?", "Calling <code>time.sleep(0.5)</code> will make your program pause its execution for one-half of a second."],
  ["Say you decide a timestamp like 1773813875.3518236 has too many digits after the decimal point. What code returns the current time rounded to the nearest second?", "<code>round(time.time())</code>"],
  ["Say you've run <code>import datetime</code>. What is wrong with the code <code>datetime.now()</code>?", "The correct function call is <code>datetime.datetime.now()</code>, not <code>datetime.now()</code>. The first datetime is a module, and the second is a data type in the datetime module, also named datetime."],
  ["If you run <code>current_time = datetime.datetime.now()</code>, how can you get an integer of the current year?", "The expression <code>current_time.year</code> evaluates to an integer of the current year."],
  ["What date and time does the datetime object from this expression represent: <code>datetime.datetime.fromtimestamp(0)</code>? (Assume you're in the UTC time zone.)", "January 1, 1970, at 12:00 AM. This timestamp is also known as the Unix epoch."],
  ["Objects of what data type represent a moment in time?", "<code>datetime.datetime</code> objects represent a moment in time."],
  ["Objects of what data type represent a duration of time?", "<code>datetime.timedelta</code> objects represent a duration of time."],
  ["The <code>datetime.timedelta()</code> function can have keyword arguments like <code>datetime.timedelta(days=11, hours=10, minutes=9, seconds=8)</code>. Why can't you specify the number of months or years?", "Because different months and years contain different numbers of days, hours, minutes, and seconds."],
  ["What code creates a timedelta object that represents 1,000 days?", "<code>datetime.timedelta(days=1000)</code>"],
  ["What code creates a timedelta object that represents 2,000 days? (Hint: reuse the code for creating 1,000 days.)", "<code>datetime.timedelta(days=1000) * 2</code> or <code>datetime.timedelta(days=1000) + datetime.timedelta(days=1000)</code>"],
  ["If you add a <code>datetime.datetime</code> object to a <code>datetime.timedelta</code> object, what is the data type of the evaluated result?", "Adding a <code>datetime.datetime</code> object to a <code>datetime.timedelta</code> object evaluates to a <code>datetime.datetime</code> object."],
  ["If you add a <code>datetime.timedelta</code> object to a <code>datetime.timedelta</code> object, what is the data type of the evaluated result?", "Adding a <code>datetime.timedelta</code> object to a <code>datetime.timedelta</code> object evaluates to a <code>datetime.timedelta</code> object."],
  ["Assume you run <code>from datetime import timedelta</code>. What does the expression <code>timedelta(seconds=15) - timedelta(seconds=5) == timedelta(seconds=10)</code> evaluate to?", "The expression evaluates to <code>True</code>."],
  ["Assume you run <code>from datetime import timedelta</code>. Does <code>timedelta(seconds=15) + 5 == timedelta(seconds=20)</code> evaluate to <code>True</code>?", "You cannot add a timedelta object to an integer, so this expression raises an exception."],
  ["What does <code>datetime.timedelta(seconds=60) == datetime.timedelta(minutes=1)</code> evaluate to?", "The expression evaluates to <code>True</code>."],
  ["What does the 'f' in <code>strftime()</code> stand for?", "Format."],
  ["What does the 'p' in <code>strptime()</code> stand for?", "Parse."],
  ["Which function takes a date and time as a human-readable string, along with a string to parse it, and then returns a <code>datetime.datetime</code> object: <code>strftime()</code> or <code>strptime()</code>?", "<code>strptime()</code> takes a human-readable string of a date and time along with a string to parse it, then returns a <code>datetime.datetime</code> object."],
  ["Which function returns a date and time as a human-readable string: <code>strftime()</code> or <code>strptime()</code>?", "<code>strftime()</code> returns a human-readable string of a date and time."],
  ["Does <code>datetime.datetime.strptime('26', '%y')</code> evaluate to a datetime object with the year 1926 or 2026?", "The code returns a datetime object with the year 2026."],
  ["Does <code>datetime.datetime.strptime('76', '%y')</code> evaluate to a datetime object with the year 1976 or 2076?", "The code returns a datetime object with the year 1976."],
  ["What datetime object does <code>datetime.datetime.strptime(\"October of '26\", \"%B of '%y\")</code> evaluate to?", "<code>datetime.datetime(2026, 10, 1, 0, 0)</code>"],
  ["What does the expression <code>datetime.timedelta(days=0, hours=0, minutes=1, seconds=5).total_seconds()</code> evaluate to?", "<code>65.0</code>"],
  ["What is the difference between a process and a program?", "A process is a running instance of a program. If you have a calculator program on your computer running multiple times, each of those instances is a separate process of the same program."],
  ["What is the data type of the argument passed to <code>subprocess.run()</code>?", "The <code>subprocess.run()</code> function takes a list of strings representing the program to run and its command line arguments."],
  ["Why doesn't <code>subprocess.run(['/System/Applications/Calculator.app'])</code> run the calculator app on macOS?", "Unlike on Windows and Linux, on macOS you must run the <code>open</code> program followed by the program's filepath; you can't specify the filepath alone."],
  ["How is the <code>subprocess.Popen()</code> function different from the <code>subprocess.run()</code> function?", "The <code>subprocess.Popen()</code> function launches a program and then immediately returns, while the <code>subprocess.run()</code> function launches a program and doesn't return until the launched program quits."],
  ["The <code>subprocess.run()</code> and <code>subprocess.Popen()</code> functions return Popen objects. What does the <code>poll()</code> method of Popen objects return if the launched program is still running at the time of the method call?", "The <code>poll()</code> method returns <code>None</code> if the launched program is still running."],
  ["What does the <code>poll()</code> method of Popen objects return if the launched program has quit?", "The <code>poll()</code> method returns the integer exit code if the launched program has quit."],
  ["If a program has quit without errors, what is its exit code?", "A program that quits without error has an exit code of 0."],
  ["What happens when you call a Popen object's <code>kill()</code> method?", "The <code>kill()</code> method quits the launched program."],
  ["What happens when you call a Popen object's <code>wait()</code> method?", "The <code>wait()</code> method blocks and won't return until the launched program has quit."],
  ["Write the code to open a hello.txt file using the default application for opening .txt files on Windows.", "<code>subprocess.run(['start', 'hello.txt'], shell=True)</code>"],
  ["Write the code to open a hello.txt file using the default application for opening .txt files on macOS and Linux.", "<code>subprocess.run(['open', 'hello.txt'])</code>"],
  ["Write the code to run a Python script named spam.py.", "<code>subprocess.run(['python', 'spam.py'])</code> (or 'python3' on macOS and Linux)"]
];
