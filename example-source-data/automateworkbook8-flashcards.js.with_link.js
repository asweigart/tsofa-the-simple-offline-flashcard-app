const TOPIC = "Automate the Boring Stuff Workbook, Chapter 8 - Strings and Text Editing";
let JUSTIFICATION = "left"; // "left", "right", or "center"
let FLASHCARDS = [
  ["What is a string literal?", "A string literal is the string value literally in the source code, typed as text surrounded by quote characters."],
  ["What is the difference between string literals using single quotes and strings using double quotes?", "They are the same, though single-quoted strings need to escape any single-quote characters that are a part of the string, while double-quoted strings need to escape any double-quote characters that are a part of the string."],
  ["How do you mark the start and end of a multiline string?", "Multiline strings start and end with a \"triple quote,\" which is either three single-quote characters or three double-quote characters."],
  ["Is <code>\"Zophie's scratching post\"</code> valid Python code for a string?", "Yes. It is valid. String literals that begin and end with double quotes don't have to escape single-quote characters."],
  ["Is <code>\"Zophie\\'s scratching post\"</code> valid Python code for a string?", "Yes. It is valid. String literals that begin and end with double quotes don't have to escape single-quote characters."],
  ["Are escape characters needed when a string contains both single- and double-quote characters?", "Yes. If the string uses both single and double quotes, then one of the quote types must be escaped. (The exception is that multiline strings can contain both kinds of quotes unescaped.)"],
  ["Why are the string literals <code>'A\\'B'</code> and <code>'A\\\\\\'B'</code> valid, but not the string literal <code>'A\\\\'B'</code>?", "The 'A\\'B' string literal is valid because it has an escaped single-quote character \\'. The 'A\\\\\\'B' string literal is valid because it has an escaped backslash \\\\ followed by an escaped single quote \\'. But 'A\\\\'B' is invalid because the first backslash escapes the second backslash, leaving the following quote unescaped."],
  ["How do you mark a string literal as a raw string literal?", "You can mark a string literal as raw with an r prefix: <code>r'Hello'</code>."],
  ["How many backslashes appear when you run the code <code>print('A\\\\B')</code>?", "One backslash, because \\\\ is an escape character for backslashes (just as \\n is an escape character for newlines and \\t is an escape character for tabs)."],
  ["How many backslashes appear when you run the code <code>print(r'A\\\\B')</code>?", "Two backslashes, because raw string literals (marked with the r prefix) don't escape any characters and treat all backslashes as literal backslash characters in the string."],
  ["How can you create multiline comments without using a # character at the start of each line?", "A multiline string (which begins and ends with three quote characters) can also function as a multiline comment, as a string by itself does nothing in Python."],
  ["What does <code>'Hello'[1]</code> evaluate to?", "'e' because 0 is the index of the first character in a string and 1 is the index of the second character."],
  ["What does <code>'Hello'[-1]</code> evaluate to?", "'o' because negative indexes count from the end of the string, with -1 being the index of the last character."],
  ["What does <code>'Hello'[4:5]</code> evaluate to?", "'o' because 4:5 is a slice substring starting at index 4 and going up to, but not including, index 5."],
  ["What does <code>'Hello'[4:4]</code> evaluate to?", "The blank string '' because the slice substring starts at index 4 and goes up to, but does not include, index 4. A slice substring with the same starting and ending indexes always translates to an empty string."],
  ["Does <code>'Hello'[9999]</code> cause an IndexError?", "Yes. 'Hello'[9999] causes an IndexError because the largest index in the string 'Hello' is 4, and 9999 is larger than 4."],
  ["Does <code>'Hello'[1:9999]</code> cause an IndexError?", "No. Slices don't cause errors in Python. Instead, because 9999 is larger than the largest index, the slice just goes all the way to the end of the string. 'Hello'[1:9999] evaluates to 'ello'."],
  ["What does <code>'H' in 'Hello'</code> evaluate to?", "True, because 'H' does appear in 'Hello'. You can consider this code to be the same as the expression 'H' in ['H', 'e', 'l', 'l', 'o']."],
  ["What does <code>'H' in ['Hello', 'Goodbye']</code> evaluate to?", "False. Only 'Hello' and 'Goodbye' exist in this list."],
  ["What does <code>'Hello' in ['Hello', 'Goodbye']</code> evaluate to?", "True because 'Hello' is one of the two values in that list."],
  ["What does <code>'Hello' in ['Hi', ['Hello', 'Goodbye']]</code> evaluate to?", "False because 'Hello' is neither the string value 'Hi' nor the list value ['Hello', 'Goodbye']."],
  ["What does <code>['Hello', 'Goodbye'] in ['Hi', ['Hello', 'Goodbye']]</code> evaluate to?", "True because the list ['Hello', 'Goodbye'] is the second value (after 'Hi') in the other list."],
  ["Why does <code>'I am number ' + 42</code> cause an error while <code>'I am number ' + str(42)</code> does not?", "'I am number ' + 42 causes an error because you are trying to add a string and an integer. 'I am number ' + str(42) doesn't cause an error, because str(42) evaluates to the string '42', and concatenating two string values is allowed."],
  ["Does <code>f'I am number {42}'</code> cause an error?", "No. The f-string literal doesn't cause an error, because even though 42 is an integer, the f-string syntax automatically converts it to a string value."],
  ["Does <code>f'I am number {str(42)}'</code> cause an error?", "No. The f-string literal doesn't cause an error when it puts the string that str(42) returns inside the f-string. While having str() is unnecessary, it doesn't cause an error. The curly brackets inside an f-string can contain not just individual variables but entire expressions."],
  ["Describe the difference between what <code>print(beard_length)</code> and <code>print(f'{beard_length=}')</code> display on the screen.", "While beard_length just evaluates to the value in the variable, the f-string f'{beard_length=}' evaluates to 'beard_length=' followed by the string representation of the value. It's a common way to print the name and contents of a variable for debugging purposes."],
  ["If f-strings are the preferred way of putting strings inside other strings, why do you need to learn about string interpolation and the format() string method?", "You still need to learn about string interpolation and the format() string method because you may read them in Python code written by other people. (Also, f-strings were introduced in Python 3.6 and aren't available in earlier versions.)"],
  ["Can the expression <code>spam.upper() == 'hello'</code> ever evaluate to True?", "No. Calling the upper() method on a string ensures that it will never have lowercase letters. This expression can only evaluate to False no matter what string is in spam."],
  ["What does <code>'42'.isupper()</code> evaluate to?", "False. The string must have at least one uppercase letter for the isupper() method to return True."],
  ["What does <code>'X42'.isupper()</code> evaluate to?", "True. If the string has at least one uppercase letter and no lowercase letters, the isupper() method returns True."],
  ["What are the data types of the return values of the lower() and islower() methods?", "The lower() method returns a string of the original string's letters but in lowercase. The islower() method returns a True or False Boolean value depending on the string's casing."],
  ["What does <code>'This sentence is capitalized.'.istitle()</code> return?", "False, because at least one word in that string doesn't begin with an uppercase letter."],
  ["What does <code>'This sentence is capitalized.'.title()</code> return?", "'This Sentence Is Capitalized.'"],
  ["Write an expression that determines whether the string in spam contains only numeric digits.", "<code>spam.isdecimal()</code> is the expression that evaluates to True if spam contains only numeric digits. (The expression spam.isdigit() also works.)"],
  ["What does <code>'1,000,000'.isdecimal()</code> return?", "False because the commas are not decimal digits."],
  ["What does <code>'-5'.isdecimal()</code> return?", "False because the negative sign is not a decimal digit."],
  ["What does <code>str(float(42))</code> return?", "The string '42.0' because float(42) returns the floating-point value 42.0, and str(42.0) returns '42.0'."],
  ["What does <code>str(float(42)).isdecimal()</code> return?", "False because the period in '42.0' is not a decimal digit."],
  ["What is the difference between <code>'headache'.startswith('he') and 'headache'.endswith('he')</code> and <code>'headache'.startswith('he').endswith('he')</code>?", "The first evaluates to True and True, which further evaluates to True. The second evaluates to True.endswith('he'), which causes an error, because endswith() is a string method and cannot be called on Boolean values like True."],
  ["What is the data type of the join() string method's return value?", "The join() string method returns string values."],
  ["What is the data type of the split() string method's return value?", "The split() string method returns a list value (specifically, a list of string values)."],
  ["What does <code>','.join(['cat', 'dog', 'moose'])</code> evaluate to?", "'cat,dog,moose'"],
  ["What does <code>','.join('cat,dog,moose')</code> evaluate to?", "'c,a,t,,,d,o,g,,,m,o,o,s,e' because the join() method expects a sequence of values. If you accidentally pass a string instead of a list of strings, the joining string is put in between the characters of the string."],
  ["What string method should you call on the string 'Hello!' to return the 10-character string padded with spaces '    Hello!'?", "Calling <code>'Hello!'.rjust(10)</code> returns '    Hello!'."],
  ["What string method should you call on the string 'Hello!' to return the 10-character string padded with spaces 'Hello!    '?", "Calling <code>'Hello!'.ljust(10)</code> returns 'Hello!    '."],
  ["What is a text character's Unicode code point?", "A Unicode code point is a number that identifies a text character."],
  ["What Unicode encoding should you almost certainly use when writing programs?", "The UTF-8 encoding is almost certainly the Unicode encoding you want to use in every case."],
  ["What function returns a text character string, given a Unicode code point integer?", "chr() returns a text character string of the Unicode code point integer you pass it."],
  ["What function returns a Unicode code point integer, given a text character string?", "ord() returns the Unicode code point integer of the text character string you pass it."],
  ["Given that the expression <code>ord('!') < ord('A')</code> evaluates to True, which comes first in \"ASCIIbetical\" order, ! or A?", "The ! comes first before the A in ASCIIbetical order because the exclamation point's code point integer is less than the uppercase letter A's code point integer."],
  ["Is pyperclip a built-in package that comes with Python?", "No. pyperclip is a third-party module and doesn't come with Python."],
  ["Which function returns a string: pyperclip.copy() or pyperclip.paste()?", "The pyperclip.paste() function returns a string of the text contents on the clipboard."],
  ["Which function takes a string argument: pyperclip.copy() or pyperclip.paste()?", "The pyperclip.copy() function takes a string argument of text to place on the clipboard."],
  ["If you call <code>pyperclip.copy('Hello')</code> and then call <code>pyperclip.copy('Goodbye')</code>, what does <code>pyperclip.paste()</code> return?", "'Goodbye', because 'Goodbye' replaced 'Hello' as the text stored on the clipboard."]
];
